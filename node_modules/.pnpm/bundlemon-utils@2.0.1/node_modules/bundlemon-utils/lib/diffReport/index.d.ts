import { Status } from '../consts';
import type { FileDetails, FileDetailsDiff, DiffReport, DiffStats, DiffReportInput } from '../types';
interface GenerateDiffFilesResult {
    files: FileDetailsDiff[];
    stats: DiffStats;
    status: Status;
}
export declare function calcDiffFiles(currFiles: FileDetails[], baseFiles?: FileDetails[]): GenerateDiffFilesResult;
export declare function generateDiffReport(currInput: DiffReportInput, baseInput?: DiffReportInput): DiffReport;
export {};
