"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const bundlemon_utils_1 = require("bundlemon-utils");
const logger_1 = __importDefault(require("../common/logger"));
const analyzer_1 = require("./analyzer");
const outputs_1 = require("./outputs");
const report_1 = require("./report");
const initializer_1 = require("./initializer");
exports.default = async (config) => {
    const normalizedConfig = await (0, initializer_1.initializer)(config);
    if (!normalizedConfig) {
        throw new Error('Failed to initialize');
    }
    const { files, groups } = await (0, analyzer_1.analyzeLocalFiles)(normalizedConfig);
    if (files.length === 0 && groups.length === 0) {
        throw new Error('No files or groups found');
    }
    const report = await (0, report_1.generateReport)(normalizedConfig, { files, groups });
    if (!report) {
        throw new Error('Failed to generate report');
    }
    await (0, outputs_1.generateOutputs)(report);
    logger_1.default.info(`Done - ${report.status === bundlemon_utils_1.Status.Pass ? 'Success' : 'Failure'} - ${(0, bundlemon_utils_1.getReportConclusionText)(report)}`);
    return report;
};
