import type { FileDetails } from 'bundlemon-utils';
import type { NormalizedConfig } from '../types';
export declare function analyzeLocalFiles(config: NormalizedConfig): Promise<{
    files: FileDetails[];
    groups: FileDetails[];
}>;
