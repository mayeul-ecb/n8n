import type { FileDetails } from 'bundlemon-utils';
import type { NormalizedFileConfig, PathLabels } from '../types';
interface GetFilesDetailsParams {
    baseDir: string;
    pathLabels: PathLabels;
    config: NormalizedFileConfig[];
    allFiles: string[];
    stopOnMatch: boolean;
}
export declare function getFilesDetails({ baseDir, pathLabels, config, allFiles, stopOnMatch, }: GetFilesDetailsParams): Promise<FileDetails[]>;
export declare function groupFilesByPattern(files: FileDetails[]): FileDetails[];
export {};
