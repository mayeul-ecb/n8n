import type { NormalizedConfig } from '../types';
import type { Report } from 'bundlemon-utils';
export declare class OutputManager {
    private outputs;
    private outputCreator;
    initOutputs: (config: NormalizedConfig) => Promise<void>;
    generateOutputs: (report: Report) => Promise<void>;
    getOutputs: () => OutputManager['outputs'];
}
export declare const initOutputs: (config: NormalizedConfig) => Promise<void>, generateOutputs: (report: Report) => Promise<void>;
